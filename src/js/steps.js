export const steps = {
	clientData: 0,
	addressData: 1,
	summary: 2,
};

export let currentStep = 0;

// const nextStep = () => {
// 	if (currentStep < 2) {
// 		currentStep++;
// 	}
// };

// const previousStep = () => {
// 	if (currentStep > 0) {
// 		currentStep--;
// 	}
// };
